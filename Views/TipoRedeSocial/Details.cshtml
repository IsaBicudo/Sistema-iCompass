@model iCompass.Models.TipoRedeSocial

@{
    ViewData["Title"] = "Detalhes";
}

<h1>Detalhes</h1>

<div>
    <h4 class="subtitulo">Tipo de Rede Social</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NomeTipoRedeSocial)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NomeTipoRedeSocial)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" class="btn btn-primary" asp-route-id="@Model?.TipoRedeSocialId"><i class="fa-solid fa-pen-to-square"></i></a>
    <a asp-action="Index" class="btn btn-primary">Voltar à Lista</a>
</div>
