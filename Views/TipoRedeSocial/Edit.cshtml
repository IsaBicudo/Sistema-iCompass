@model iCompass.Models.TipoRedeSocial

@{
    ViewData["Title"] = "Editar";
}

<h1>Editar</h1>

<h4 class="subtitulo">Tipo de Rede Social</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TipoRedeSocialId" />
            <div class="form-group">
                <label asp-for="NomeTipoRedeSocial" class="control-label">Rede Social</label>
                <input asp-for="NomeTipoRedeSocial" class="form-control" />
                <span asp-validation-for="NomeTipoRedeSocial" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Salvar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-primary">Voltar à Lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
