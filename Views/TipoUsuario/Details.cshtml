@model iCompass.Models.TipoUsuario

@{
    ViewData["Title"] = "Detalhes";
}

<h1>Detalhes</h1>

<div>
    <h4 class="subtitulo">Tipos de Usuário</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NomeTipoUsuario)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NomeTipoUsuario)
        </dd>
    </dl>
</div>
<div>
    <a class="btn-primary" asp-action="Edit" asp-route-id="@Model?.TipoUsuarioId"><i class="fa-solid fa-pen-to-square"></i></a>
    <a class="btn-primary" asp-action="Index">Voltar à Lista</a>
</div>
