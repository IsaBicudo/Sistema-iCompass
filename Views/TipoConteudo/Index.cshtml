@model IEnumerable<iCompass.Models.TipoConteudo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeTipoConteudo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PublicoAlvo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeTipoConteudo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PublicoAlvo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoConteudoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoConteudoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoConteudoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
